---
name: evaluate-techniques
description: >-
  講義由来の技法が「でたらめな売買」より良いかを、毎回同じ条件で測る。単独の技法と、
  複数が揃った局面（合流）の両方を評価し、修正の前後を突き合わせて差分を出す。
  「技法を評価して」「合流で測って」「指標を直したので測り直して」「エッジがあるか確かめて」
  「前と比べてどう変わった」のようなリクエストで使用。扱うのは mcp-tradingview の
  `tradingview-signal-study` と、その出力 JSON の突き合わせ。
  Do NOT use for: 銘柄の絞り込み（screen-margin-improving-stocks を使う）、
  発注、前向き台帳の集計（tradingview_ledger_report）、技法ノートの起案。
compatibility: >-
  mcp-tradingview と mcp-invest-knowledge のクローンが要る。価格は外部提供元から取得し
  `-cache-dir` に固定されるので、同じ cache なら結果は再現する。読むだけで何も発注しない。
allowed-tools: Bash, Read, Glob, Grep
---

# 技法を同じ条件で評価する

## CRITICAL: 先に読むこと

- **既定値のまま合流を測らない。** `-levels` の既定 `1,2,3,4` では**関門が何も絞らない**ことが
  実測で分かっている（全水準で試行数が同じ 109 件）。必ず [合意の水準](#合意の水準を決める) を読む。
- **「差が無い」を結果として受け取らない。** 比較が成立しているかを先に確かめる。
  付属の `compare.py` は項目が読めなければ**エラーで落ちる**。落ちたら直すまで報告しない。
- **基準（alpha 0.05）を結果を見てから動かさない。** 動かすなら測る前に決めて記録する。
- **「立証」と書かない。** 判定語は `outperformed_matched_null`（比較対象を上回った）まで。
- **q 値を必ず併記する。** p 値だけ見ると、29 個試して偶然 1 個当たったものを発見と呼んでしまう。

## 用語

| 正式名 | 意味 |
| --- | --- |
| 比較対象 | 売買回数・保有期間・銘柄を揃えた**でたらめな売買**（matched null）。入る日付だけが違う |
| p 値 | でたらめな売買 1000 通りのうち、技法以上だったものの割合 |
| q 値 | **同じ基準で拾った中に偽物が混じる割合**（多重検定の補正・BH-FDR） |
| 合流 | 複数の技法が同時に成立している状態。講義の「単独で使わない」に対応 |
| 合意の水準 k | 何個の技法が揃ったら候補とするか |
| 反対票 | 売り技法が成立していること。買い候補はこれが 0 のときだけ |
| 単調性 | 合意が増えるほど成績が上がるか。合流の本丸 |

## 目次

- [用語](#用語)
- [ユースケース](#ユースケース)
- [前提条件](#前提条件)
- [手順](#手順)
- [合意の水準を決める](#合意の水準を決める)
- [出力](#出力)
- [チェックリスト](#チェックリスト)
- [Troubleshooting](#troubleshooting)

## ユースケース

### 1. 指標を直したので測り直す

- **Trigger**: 「RSI を直したので影響を測って」「物差しを直した前後を比べて」
- **Steps**: 修正前後の実行ファイルを作る → 同じ cache・同じ種で両方走らせる → `compare.py`
- **Result**: 試行数・p 値・q 値・判定がどう動いたかの表。**判定が動かなくても入力が動いていれば書く**

### 2. 合流で評価する

- **Trigger**: 「合流で測って」「複数の技法を掛け合わせた場合は？」
- **Steps**: 水準を決める（下記）→ `-confluence-state` で走らせる → 単調性と各水準を読む
- **Result**: 合意が増えるほど成績が上がるかの判定と、水準ごとの表

### 3. 単独の技法を一通り測る

- **Trigger**: 「技法にエッジはある？」「一通り評価して」
- **Steps**: 既定の設定で走らせる → 判定の内訳を出す
- **Result**: 技法ごとの判定と、上回ったものの一覧

## 前提条件

| 要る物 | 確認方法 |
| --- | --- |
| mcp-tradingview | `ls ~/dev/me/mcp-tradingview/data/universe` |
| 技法ノート | `ls ~/dev/me/mcp-invest-knowledge/sources/technique-notes` |
| 外部接続 | 価格の取得。**`-cache-dir` を指定すれば 2 回目以降は不要** |

## 手順

### ステップ 1: 実行ファイルを作る

**`go run` を使わない。** 背景で走らせると親が終わったときに扱いが不安定になり、
**出力が無いのに「終了」と見える**ことがある。先に実行ファイルにする。

```bash
cd ~/dev/me/mcp-tradingview
go build -o /tmp/study-bin ./cmd/tradingview-signal-study
```

前後を比べるときは、**両方を別の名前で作り、中身が違うことを確かめる**。

```bash
md5 -q /tmp/study-before /tmp/study-after   # 同じなら比較になっていない
```

### ステップ 2: 走らせる

**cache と種を固定する。** これを揃えないと、差が修正由来か揺らぎ由来か分からない。

```bash
/tmp/study-bin \
  -universe ~/dev/me/mcp-tradingview/data/universe/mvp-mixed.json \
  -notes-dir ~/dev/me/mcp-invest-knowledge/sources/technique-notes \
  -cache-dir /tmp/study-cache -out-dir /tmp/study-out
```

合流を見るときは `-confluence-state` と `-levels` を足す（水準は次節）。
20 銘柄で **15〜25 分**かかる。背景で走らせ、**実行中のものを数えて**二重起動を防ぐ。

```bash
ps aux | grep study-bin | grep -v grep | wc -l
```

### ステップ 3: 突き合わせる

```bash
python3 ~/.claude/skills/evaluate-techniques/scripts/compare.py \
  <前の結果.json> <後の結果.json> technique      # または confluence
```

**落ちたら直すまで報告しない。** 落ちる理由は項目名の取り違えか、走らせ方の誤り。

## 合意の水準を決める

**ここが最も間違えやすい。**

`-levels` の既定は `1,2,3,4` だが、**状態ベースではこの範囲が何も絞らない**。
合図が出た瞬間ではなく「強気の状態にある」技法を数えるので、**4 個揃うのは普通**だから。

実測（20 銘柄・2026-09-26）:

| 水準 | 試行数 |
| --- | --- |
| 1 | 109 |
| 2 | 109 |
| 3 | 109 |
| 4 | 109 |

**全部同じ＝関門が効いていない。** この結果を読んではいけない。

| 使う方式 | 既定で足りるか | 推奨 |
| --- | --- | --- |
| `-confluence-state`（状態） | **足りない** | `-levels 4,5,6,7,8,9,10` から始める |
| `-confluence`（合図の瞬間） | 足りる | 既定のまま |

**水準を上げても試行数が減らないなら、さらに上げる。** `compare.py` はこれを検知して警告を出す。

## 出力

表のあとに、**次の 5 点を必ず書く**。

1. **判定の内訳**（4 つの判定語ごとの件数。前後で比べるなら両方）
2. **入力が動いたか**（試行数の変化。判定が動かなくても入力が動いていれば書く）
3. **q 値**（p 値だけ出さない。29 個試せば偶然 1 個は 0.05 を切る）
4. **合流の単調性**（合意が増えるほど成績が上がるか。合流を測ったときだけ）
5. **射程**（curated universe・生存者バイアス・手数料と滑りは入っていない）

### 入出力の例

**入力**: 「RSI を直したので影響を測って」

**出力**:

```text
判定: 前 有意でない 15 / 上回った 3     後 有意でない 14 / 上回った 3
入力: 6 技法で試行数が変化（最大 -24%）。ma-deviation-rate は p 0.889 → 0.063
q 値: ma-deviation-rate の q は 0.365（29 個の中では、まだありふれた数字）
射程: 20 銘柄・手数料と滑りを含まない
```

**入力**: 「合流で差が出なかったので終わり」

**出力**: 終わらない。**全水準で試行数が同じなら関門が効いていない**ので、水準を上げて測り直す。

## チェックリスト

- [ ] `go build` で実行ファイルを作った（`go run` を背景で使っていない）
- [ ] 前後を比べるなら、2 つの実行ファイルの中身が違うことを確かめた
- [ ] `-cache-dir` と種を両方の実行で揃えた
- [ ] 二重起動していないことを確かめた
- [ ] 合流なら `-levels` を上げた（既定のままにしていない）
- [ ] **水準を上げると試行数が減ることを確かめた**
- [ ] `compare.py` が落ちずに通った
- [ ] 出力の 5 点をすべて書いた
- [ ] 基準（alpha）を結果を見てから動かしていない

## Troubleshooting

### 全水準で試行数が同じ

**関門が効いていない。** `-levels` を上げる。状態ベースでは 4 でも足りないことが実測で分かっている。
この状態の結果は「差が無い」ではなく「**測れていない**」。

### `compare.py` が「項目がありません」で落ちる

**正しい動き。** 存在しない項目名を比べると全件一致に見えるので、先に落としている。
エラーが読める項目の一覧を出すので、そこから選び直す。

### 出力ファイルができないのに「終了」と出る

`go run` を背景で走らせると起きる。**ログに読み込み完了までしか残らず、エラーも出ない。**
ステップ 1 のとおり実行ファイルにしてから走らせる。

### 同じ実行が二重に走っている

前の実行が生きているのに、確認コマンドが合致せず「終了」と読んだ場合に起きる。
`ps aux | grep study-bin` で**引数まで**見て数える。名前だけでは区別できない。

### `insufficient_power` と `not_evaluable` を混同しそう

| 判定語 | 意味 | データを増やせば解決するか |
| --- | --- | --- |
| `insufficient_power` | 試行数や銘柄数が足りない | **する** |
| `not_evaluable` | そもそも評価できない（スキャン専用の指標など） | **しない** |

混ぜると「もっとデータを集めれば結論が出る」と誤読させる。

## 参照

- 統計の設計と判定語: `~/dev/me/mcp-tradingview/docs/signal-study.md`
- 合流の設計: 同上の「コンフルエンス」節
